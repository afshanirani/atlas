# Changelog — ia-mapping

Every prompt change gets an entry. `agent_run_id` values in `mapping.csv` resolve here.

## 0.1.0 — 2026-08-17

Initial version. Establishes:

- grain-first method, with ambiguity raised as an open question rather than inferred
- mandatory `discovery_ref` citation per proposed row
- conflicts surfaced, never silently resolved
- deterministic `transform_rule` requirement — prose rules treated as a defect
- honest confidence scoring across the full range

## Runs

| Run ID | Date | Subject | Prompt version | Rows proposed | Notes |
|---|---|---|---|---|---|
| | | | | | |
